import asyncssh
import inspect
import asyncio

async def test():
    # just create a mock to see the return type
    # actually we can't easily instantiate SFTPClientFile, but we can look at the source
    pass
